# MUST READ THE HANDBOOK CHAP 5, SETS
s = {1, "Ayan", 2, 5, 2, 99, 100}

# print(s, type(s))


# Add Method
s.add(22)
print(s)

"""
len(s): Returns 4, the length of the set  (Length pata krne k liye)
s.remove(8): Updates the set s and removes 8 from s. ( Remove kr sakte hai particular value. )
s.pop(): Removes an arbitrary element from the set and return the element removed.
s.clear(): empties the set s.
s.union({8,11}): Returns a new set with all items from both sets.
s.intersection({8,11}): Returns a set which contains only item in both sets {8}

"""