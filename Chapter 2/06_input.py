# here we can take inputs from the user 

a = input("Write any Number: ")
b = input("Write another Number: ")

print("the combine wala Num is: ", a+b) # NOW as we can see we're taking numbers as an input but... the ans is combine of them. becuase the we are taking them as an string. 


# now if we want the ans in integers, like a proper sum so here is its example. we are converting the input that is str into int so we are getting their sum.

c = int(input("Write any Number to sum: "))
d = int(input("Write another Number to sum: "))

# now we will get ans in their sums,

print("The sum of ", c, "&", d, "is this: ", c+d)